-- test binary operators in expression lists

-- the close paren here does not change line mappings
local a, b, c = 1, 2, 3, (a *5
                          );

local d = d;
local e = e;


e = 1, ((a + 12) * (b + 12)) ^ ((a + 12) * (b + 12)
                                );
