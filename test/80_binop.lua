-- test binary operators

local b = b + c;

b = b + a;

local c = c - d + e;
local d = d - (e + f);

local e = a + 1;
local f = f / 2;

local g = 1 + g;
local h = 1 * g;

f = 2 - (a * b);

b = 2 / (a - b);

c = c ^ d ^ e;


local i = ((i + 12) * (j + 12)) ^ ((i + 12) * (j + 12));
local j = 12;
local k = 34;

local l = a + 1;

local m = (-m + 12) * 
(n + 12);
