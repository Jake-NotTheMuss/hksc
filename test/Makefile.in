# makefile for testing Lua

TOP=..
SRCDIR=src
TOPSRC=$(TOP)/$(SRCDIR)

# HKSC should be set to ../hksc or ../hkscsh by the parent Makefile
HKSC=echo you need to set HKSC in the parent Makefile: $$\(HKSC\) && false

TESTS=version

test:
	@$(MAKE) --no-print-directory -s clean
	@$(MAKE) --no-print-directory -s -r $(TESTS)

version:
	$(HKSC) --version

clean:

.PHONY: test clean version
